input {
  beats {
    port => 5044
  }
}

filter {

}

output {
  opensearch {
    hosts => ["http://opensearch:9200"]
    index => "forex-data-collector-%{+YYYY.MM.dd}"
    user => "admin"
    password => "Yusuf.32Bits"
  }
}
